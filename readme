# 1. Напишіть консольну програму, яка створює структуру даних, в якій зберігається 1000 полів (назва продуктів та цін). 

# Виведіть у консоль час створення цього об'єкта та час звернення до 50-го елемента структури даних.
#3.Створіть http-сервер із CRUD для облікових записів та генерацією токенів у форматі “ім'я-токен”. При цьому, щоб тільки обліковий запис із правами “Адмін” міг видаляти записи (роль адміна, пароль та логін достатньо передати параметром запиту).

; Ендпоінти:

; одержати список облікових записів (запис);

; створити запис;

; змінити запис;

; видалити запис;

; одержати список токенів облікового запису;

; одержати список всіх токенів;

; створити токен;

; змінити токен;

; видалити токен.
#приклад
# curl -X POST -H "Content-Type: application/json" -d '{
#  "username": "joanna",
#  "password": "secret_password",
#  "role": "user"
#}' http://localhost:3000/accounts
